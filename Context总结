1、能不能重复读取 HTTP 协议的 Body 内容？
原生 API 是不可以的。但是我们可以通过封装来允许重复读取，核心步骤是我们将 Body 读取出来之后放到一个地方，后续都从这个地方读取。

2、能不能修改 HTTP 协议的响应？
原生 API 也是不可以的。但是可以用我们的RespData 这种机制，在最后再把数据刷新到网络中，在刷新之前，都可以修改。

3、Form 和 PostForm 的区别。
课程上讲过，一个无聊的问题，正常的情况下你的API 优先使用Form就不太可能出错，Form可以读取url里的查询参数也可以获取body里的参数，但是PostForm只能获取body里的

4、Web 框架是怎么支持路径参数的？
简单得很，Web 框架在发现匹配上了某个路径参数之后，将这段路径记录下来作为路径参数的值，这个值默认是 string 类型，用户自己有需要就可以转化为不同的类型。
